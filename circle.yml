machine:
  services:
    - docker

dependencies:
  override:
    # Set up Google Cloud CLI
    - alias gcloud='sudo /opt/google-cloud-sdk/bin/gcloud'
    - echo $GCLOUD_SERVICE_KEY | base64 --decode --ignore-garbage > ${HOME}/gcloud-service-key.json
    - gcloud --quiet components update
    - gcloud auth activate-service-account --key-file ${HOME}/gcloud-service-key.json
    - gcloud config set project $GCLOUD_PROJECT

compile:
  override:
    # Use Google Container Builder to build image
    - gcloud container builds submit --tag us.gcr.io/$GCLOUD_PROJECT/venue-api:$CIRCLE_SHA1 .
  post:
    # Pull image locally for testing
    - gcloud docker -- pull us.gcr.io/$GCLOUD_PROJECT/venue-api:$CIRCLE_SHA1

test:
  override:
    - docker run us.gcr.io/$GCLOUD_PROJECT/venue-api:$CIRCLE_SHA1 test
    - docker run -d -p 8080:8080 us.gcr.io/$GCLOUD_PROJECT/venue-api:$CIRCLE_SHA1 # launch app in detached mode
    - curl --retry 3 --retry-delay 2 -v http://localhost:8080/_ah/health # test app is running on port 8080

deployment:
  production:
    branch: master
    commands:
      # -----------------------
      # Google Cloud Platform
      # -----------------------
      # Deploy new Cloud Endpoint if openapi.yaml modified in commit
      - |
        if $(git diff-tree --no-commit-id --name-only -r HEAD | grep -q openapi.yaml); then
          echo "Detected changes to openapi.yaml, deploying version to Cloud Endpoint..."
          # Deploy new API specification
          gcloud service-management deploy openapi.yaml
        fi;
      - | # Update app.yaml with latest config_id
        config_id=$(gcloud service-management configs list --service=venue-api.endpoints.ticket-bounty.appspot.com --limit=1 | awk 'NR==2{print $1}')
        sed -i.bak 's/^\(  config_id: \).*/\1'"$config_id"'/' app.yaml
        echo "Updated config_id in app.yaml to $config_id"
      # Deploy
      - gcloud --quiet app deploy --image-url us.gcr.io/$GCLOUD_PROJECT/venue-api:$CIRCLE_SHA1
      # Only tag container image as 'latest' once successfully deployed
      - gcloud --quiet beta container images add-tag us.gcr.io/$GCLOUD_PROJECT/venue-api:$CIRCLE_SHA1 us.gcr.io/$GCLOUD_PROJECT/venue-api:latest
